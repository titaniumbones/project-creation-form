# OAuth Relay Environment Variables
# Copy this file to .env and fill in your values
# .env is gitignored and will not be committed

# ===================
# Asana OAuth
# ===================
# Get these from https://app.asana.com/0/developer-console
ASANA_CLIENT_ID=
ASANA_CLIENT_SECRET=
REDIRECT_URI=http://localhost:8888/.netlify/functions/asana-callback

# ===================
# Google OAuth
# ===================
# Get these from https://console.cloud.google.com/apis/credentials
# 1. Create a new project (or use existing)
# 2. Enable: Google Drive API, Google Docs API, Google Slides API
# 3. Configure OAuth consent screen
# 4. Create OAuth 2.0 Client ID (Web application)
# 5. Add redirect URIs:
#    - http://localhost:8888/.netlify/functions/google-callback (local dev)
#    - https://YOUR-SITE.netlify.app/.netlify/functions/google-callback (production)
GOOGLE_CLIENT_ID=
GOOGLE_CLIENT_SECRET=
GOOGLE_REDIRECT_URI=http://localhost:8888/.netlify/functions/google-callback

# ===================
# Airtable OAuth
# ===================
# Get these from https://airtable.com/create/oauth
# 1. Create a new OAuth integration
# 2. Request scopes: data.records:read, data.records:write, schema.bases:read
# 3. Add redirect URIs:
#    - http://localhost:8888/.netlify/functions/airtable-callback (local dev)
#    - https://YOUR-SITE.netlify.app/.netlify/functions/airtable-callback (production)
AIRTABLE_CLIENT_ID=
AIRTABLE_CLIENT_SECRET=
AIRTABLE_REDIRECT_URI=http://localhost:8888/.netlify/functions/airtable-callback
